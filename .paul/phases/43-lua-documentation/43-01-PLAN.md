---
phase: 43-lua-documentation
plan: 01
type: execute
wave: 1
depends_on: ["42-01"]
files_modified:
  - norrust_love/main.lua (function docs)
  - norrust_love/draw.lua (function docs)
  - norrust_love/norrust.lua (function docs + section headers)
  - norrust_love/viewer.lua (function docs)
  - norrust_love/generate_tiles.lua (function docs)
  - norrust_love/generate_sprites.lua (function docs)
  - norrust_love/conf.lua (file header)
autonomous: true
---

<objective>
## Goal
Add function-level documentation to all ~74 undocumented functions across 7 Lua files. Follow the existing documentation style established in assets.lua, animation.lua, hex.lua, toml_parser.lua, and campaign_client.lua.

## Purpose
Complete Lua documentation enables contributors to understand the codebase. Combined with Phase 42 (Rust docs), the full engine is documented.

## Output
- All Lua functions in norrust_love/ have doc comments
- All files have file-level header comments
- Documentation style consistent with existing well-documented files
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@norrust_love/ (all .lua files — audit found 74 undocumented functions across 7 files)

## Reference Style (well-documented files)
@norrust_love/assets.lua (exemplar: full @param/@return docs)
@norrust_love/animation.lua (exemplar: purpose + params)
@norrust_love/hex.lua (exemplar: concise function docs)
@norrust_love/campaign_client.lua (exemplar: ctx pattern docs)
</context>

<acceptance_criteria>

## AC-1: All functions documented
```gherkin
Given all Lua source files in norrust_love/
When searching for function definitions without preceding comments
Then zero undocumented functions remain
```

## AC-2: All files have headers
```gherkin
Given all Lua source files in norrust_love/
When checking for file-level header comments
Then every .lua file starts with a descriptive comment block
```

## AC-3: Documentation style consistent
```gherkin
Given the existing documentation in assets.lua, animation.lua, hex.lua
When reviewing new documentation
Then new docs follow the same style (purpose line + @param where useful)
```

## AC-4: No code logic changed
```gherkin
Given documentation-only changes
When running cargo test
Then all 97 tests pass (Lua changes don't affect Rust tests)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Document core game files (main.lua, draw.lua, conf.lua)</name>
  <files>norrust_love/main.lua, norrust_love/draw.lua, norrust_love/conf.lua</files>
  <action>
    Add doc comments to all undocumented functions in these files.

    conf.lua (1 function):
    - Add file header: "Love2D configuration — window title, dimensions, and graphics settings."
    - love.conf(t): document what it configures

    draw.lua (6 undocumented functions):
    - draw_setup_hud: purpose + ctx param
    - draw_recruit_panel: purpose + ctx param
    - draw_unit_panel: purpose + ctx param
    - draw_terrain_panel: purpose + ctx param
    - draw_combat_preview: purpose + ctx param
    - draw_frame: purpose + ctx/state params (main entry point)

    main.lua (~3 undocumented Love2D callbacks + local helpers):
    - love.load: purpose (engine init, scenario list, state setup)
    - love.update(dt): purpose (animation updates)
    - love.keypressed(key): purpose (input dispatch)
    - Any undocumented local helper functions

    Style: Follow hex.lua/campaign_client.lua pattern — concise purpose line, @param only when not obvious from name.

    DO NOT modify any code logic. Only add comments.
  </action>
  <verify>
    grep -c "^--" on modified files (each should show increase)
    Verify no function keyword without preceding comment
  </verify>
  <done>AC-1 partially, AC-2 partially, AC-3 satisfied for core game files</done>
</task>

<task type="auto">
  <name>Task 2: Document FFI bindings and utility files</name>
  <files>norrust_love/norrust.lua, norrust_love/viewer.lua, norrust_love/generate_tiles.lua, norrust_love/generate_sprites.lua</files>
  <action>
    Add doc comments to all undocumented functions in these files.

    norrust.lua (~38 undocumented functions):
    - Group FFI wrapper functions with section headers (-- Engine lifecycle, -- Data loading, -- Game state, -- Actions, -- Pathfinding, -- Combat, -- Campaign)
    - Each function: brief purpose line (1 line max for simple wrappers)
    - json_decode already documented — skip
    - For trivial pass-through wrappers (e.g., M.get_state_json), a single line like "-- Return current game state as JSON string." is sufficient

    viewer.lua (8 undocumented functions):
    - build_asset_list: purpose
    - select_asset: purpose + params
    - draw_sidebar, draw_terrain_preview, draw_unit_preview, draw_controls_bar: purpose + ctx
    - love.keypressed, love.wheelmoved: purpose

    generate_tiles.lua (3 undocumented functions):
    - clamp01: purpose
    - generate_tile: purpose + params
    - run_generator: purpose

    generate_sprites.lua (21 undocumented functions):
    - draw_humanoid: purpose + params (main drawing function)
    - generate_spritesheet: purpose + params
    - Weapon drawing helpers (draw_sword, draw_spear, etc.): brief purpose
    - Unit definition tables: brief header comment per unit block

    Style: For simple pass-through wrappers, one-line comments are fine.
    For complex functions (draw_humanoid, generate_spritesheet), use multi-line with @param.

    DO NOT modify any code logic. Only add comments.
    After both tasks, run cargo test to verify no breakage.
  </action>
  <verify>
    cargo test (97 tests pass)
    grep for function definitions without preceding comments — should be 0
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any existing doc comments (only ADD, never modify)
- Any code logic, function signatures, or control flow
- norrust_core/* (Rust documentation was Phase 42)
- Well-documented files: assets.lua, animation.lua, hex.lua, toml_parser.lua, campaign_client.lua

## SCOPE LIMITS
- Documentation only — no refactoring, no new features
- Follow existing style (see assets.lua, animation.lua for reference)
- Keep doc comments concise (1-2 lines for simple functions, multi-line for complex ones)
- For FFI wrappers in norrust.lua, one-line comments are sufficient

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All .lua files have file-level header comments
- [ ] All functions have doc comments
- [ ] `cargo test` passes all 97 tests
- [ ] No existing documentation was modified
- [ ] Doc style matches existing patterns (assets.lua, animation.lua)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Zero undocumented functions remain
- 97 tests still passing
</success_criteria>

<output>
After completion, create `.paul/phases/43-lua-documentation/43-01-SUMMARY.md`
</output>
