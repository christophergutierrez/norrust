---
phase: 115-missing-units-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["data/units/lieutenant/general/general.toml"]
autonomous: true
---

<objective>
## Goal
Create the missing General unit TOML and verify all advancement chains resolve for all 4 factions.

## Purpose
The Lieutenant → General advancement chain is broken because no General TOML exists. This is the only missing unit in the entire 4-faction tree. Completing it means every advances_to reference resolves to a real unit definition.

## Output
- General unit TOML created from Wesnoth WML reference
- Verified: every advances_to target across all 114+ units resolves
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@data/units/lieutenant/lieutenant.toml (advances_to = ["General"])
@/home/chris/git_home/wesnoth/data/core/units/humans/Loyalist_General.cfg (WML reference)
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: General Unit Exists
```gherkin
Given the lieutenant advances_to = ["General"]
When the unit registry loads data/units/
Then a General unit definition is found with correct Wesnoth-sourced stats
```

## AC-2: All Advancement Chains Resolve
```gherkin
Given all unit TOMLs in data/units/
When every advances_to target is checked against the registry
Then every target resolves to an existing unit definition (zero dangling references)
```

## AC-3: Registry Loads Successfully
```gherkin
Given the General TOML is added
When cargo test is run
Then all existing tests pass with no regressions
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create General unit TOML</name>
  <files>data/units/lieutenant/general/general.toml</files>
  <action>
    Create data/units/lieutenant/general/general.toml using Wesnoth WML as reference source (/home/chris/git_home/wesnoth/data/core/units/humans/Loyalist_General.cfg).

    The General is:
    - Level 3 Loyalist leader (Lieutenant → General)
    - id = "General", abilities = ["leader", "leadership"]
    - Has sword (melee blade) and crossbow (ranged pierce) attacks
    - advances_to = ["Grand Marshal"] or [] if Grand Marshal doesn't exist in our tree

    Follow the exact TOML format of lieutenant.toml. Include all standard sections: attacks, resistances, movement_costs, defense.

    Check if Grand Marshal exists in our unit tree. If not, set advances_to = [] (terminal).
  </action>
  <verify>
    1. File exists at data/units/lieutenant/general/general.toml
    2. TOML is valid (id, name, race, level, all sections present)
    3. cargo test passes (registry loads the new unit)
  </verify>
  <done>AC-1, AC-3 satisfied: General unit exists with correct stats and registry loads it</done>
</task>

<task type="auto">
  <name>Task 2: Verify all advancement chains resolve</name>
  <files>None (verification only)</files>
  <action>
    Write a one-off verification: for every unit TOML in data/units/, check that each entry in its advances_to array matches an id field in some other unit TOML. Report any dangling references.

    This can be done via a shell script or by reading the existing integration tests. The existing test_all_units_load test in loader.rs verifies registry loading but may not check advances_to resolution.

    If any dangling references are found besides the ones we just fixed, report them.
  </action>
  <verify>
    Zero dangling advances_to references across all unit TOMLs.
  </verify>
  <done>AC-2 satisfied: Every advances_to target resolves to a real unit definition</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_core/ Rust source code (data-only phase)
- Existing unit TOMLs (no stat modifications — that's Phase 116)
- Test files (tests may reference legacy units like fighter/hero/archer — leave them)
- Scenario files

## SCOPE LIMITS
- Only create the General TOML — no other new units
- Do not remove legacy test units (fighter, hero, archer, ranger) — they're used in tests and scenarios
- No sprite work
- No stat corrections (Phase 116 handles that)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] data/units/lieutenant/general/general.toml exists with valid TOML
- [ ] cargo test passes (all existing tests + registry loads General)
- [ ] Zero dangling advances_to references
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- General unit created with Wesnoth-sourced stats
- All advancement chains resolve end-to-end
- Zero test regressions
</success_criteria>

<output>
After completion, create `.paul/phases/115-missing-units-cleanup/115-01-SUMMARY.md`
</output>
