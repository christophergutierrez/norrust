---
phase: 78-upvalue-contexts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [norrust_love/main.lua]
autonomous: false
---

<objective>
## Goal
Group main.lua's 62 file-scope local variables into context tables by concern, reducing upvalue count to ~25 and eliminating the recurring LuaJIT 60-upvalue limit problem.

## Purpose
The file has hit the 60-upvalue limit three times during development (phases 75, 76, 77), each time requiring ad-hoc workarounds. Grouping related variables into context tables is the structural fix.

## Output
- main.lua with ~25 top-level locals instead of 62
- All existing functionality preserved (syntax check + manual verification)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@norrust_love/main.lua (lines 49-134: state variables to group)
</context>

<acceptance_criteria>

## AC-1: Upvalue count reduced
```gherkin
Given main.lua currently has 62 file-scope local variables
When variables are grouped into context tables by concern
Then the file has ≤25 file-scope locals (counting context tables as 1 each)
```

## AC-2: All functionality preserved
```gherkin
Given the game works before refactoring
When context tables replace individual locals
Then luajit syntax check passes and game launches to scenario select without errors
```

## AC-3: Context tables are logically grouped
```gherkin
Given 62 variables spanning 7+ concerns
When grouped into context tables
Then each table contains only related variables (camera, ghost, campaign, selection, dialogue, scenario)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Group variables into context tables</name>
  <files>norrust_love/main.lua</files>
  <action>
    Replace individual local variable declarations (lines 49-134) with context tables. Keep these as individual locals (essential everywhere or already tables):
    - engine, game_mode, game_over, winner_faction
    - factions, faction_id, sel_faction_idx, leader_placed, next_unit_id
    - status_message, status_timer, shared, fonts
    - terrain_tiles, unit_sprites, unit_anims, dying_units, pending_anims, FACTION_COLORS
    - Constants: BOARD_COLS, BOARD_ROWS, COLOR_FLAT, UI_SCALE, CAMERA_PAN_SPEED, CAMERA_LERP_SPEED, BLUE, RED, game mode constants, SCENARIOS, CAMPAIGNS

    Create these context tables:

    1. `camera` — board_origin_x/y, camera_offset_x/y, camera_min/max_x/y, drag_active, drag_start_x/y, drag_camera_start_x/y, camera_target_x/y, camera_lerping, camera_zoom, ZOOM_MIN, ZOOM_MAX, ZOOM_STEP (18 vars → 1)

    2. `ghost` — ghost_col, ghost_row, ghost_unit_id, ghost_attackable, ghost_path (5 vars → 1)

    3. `combat_state` — already a table, keep as-is (1 var)

    4. `campaign` — campaigns_path, campaign_active, campaign_data, campaign_index, campaign_veterans, campaign_gold, campaign_roster (7 vars → 1)

    5. `selection` — selected_unit_id, reachable_cells, reachable_set, selected_recruit_idx, recruit_mode, recruit_error, recruit_state, inspect_unit_id, inspect_terrain (9 vars → 1)

    6. `dialogue` — active_dialogue, dialogue_history, show_dialogue_history, history_scroll (4 vars → 1)

    7. `scenario` — scenarios_path, scenario_board, scenario_units, scenario_preset (4 vars → 1)

    Then update ALL references throughout main.lua. For each variable that moves into a table:
    - Find every read/write of that variable
    - Replace with table.field access (e.g., ghost_col → ghost.col, campaign_active → campaign.active)
    - Be especially careful with:
      - camera variables used in get_viewport, center_camera_on_hex, love.update lerp, love.mousepressed drag, love.wheelmoved zoom
      - ghost variables used in cancel_ghost, start_ghost, commit_ghost_move, draw context building
      - campaign variables used in build_campaign_ctx, apply_campaign_ctx, campaign flow in keypressed
      - selection variables used in select_unit, cancel_ghost, build_draw_ctx
      - dialogue variables used in event subscribers and draw context

    Important:
    - Do NOT change draw.lua — it receives these values via the ctx table built in love.draw, which already uses its own field names
    - Do NOT change any module interfaces (events.lua, save.lua, etc.)
    - Do NOT rename fields in the draw ctx table — just change where values are read FROM
    - Keep the same initialization values for all variables
  </action>
  <verify>luajit -bl norrust_love/main.lua /dev/null (syntax check passes with no output)</verify>
  <done>AC-1 and AC-3 satisfied: variables grouped into 6 context tables, file-scope locals reduced to ~25</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refactored main.lua with context tables replacing 62 individual locals</what-built>
  <how-to-verify>
    1. Run: cd norrust_love && love .
    2. Select a scenario (e.g., Crossing)
    3. Move a unit (click unit, click destination, click to confirm)
    4. Attack an enemy (ghost adjacent, click enemy)
    5. End turn (E key), verify AI plays
    6. Press ? for help overlay
    7. Right-click terrain for inspection
    8. Press H for dialogue history
    9. Try zooming (scroll wheel) and panning (arrow keys / drag)
    10. Try F5 save and F9 load
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_love/draw.lua (receives state via ctx table, unaffected)
- norrust_love/sound.lua
- norrust_love/events.lua
- norrust_love/save.lua
- norrust_love/roster.lua
- norrust_love/norrust.lua
- norrust_core/ (Rust engine untouched)
- Any other .lua module besides main.lua

## SCOPE LIMITS
- Only restructure variable declarations and their references
- Do not extract functions into separate modules (that's Phase 79)
- Do not rename the draw ctx fields (draw.lua reads those)
- Do not change any game logic or behavior

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `luajit -bl norrust_love/main.lua /dev/null` passes silently
- [ ] Game launches and plays through scenario select → gameplay → combat → turn end
- [ ] File-scope local count ≤ 25
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Upvalue count reduced from 62 to ≤25
</success_criteria>

<output>
After completion, create `.paul/phases/78-upvalue-contexts/78-01-SUMMARY.md`
</output>
