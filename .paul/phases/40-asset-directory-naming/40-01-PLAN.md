---
phase: 40-asset-directory-naming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - norrust_love/assets/units/* (16 directory renames)
  - norrust_love/assets/units/*/sprite.toml (16 files, id field update)
  - norrust_love/assets.lua
  - norrust_love/generate_sprites.lua
  - MISSSION_CONTROL.md → MISSION_CONTROL.md
autonomous: false
---

<objective>
## Goal
Rename all 16 unit asset directories from PascalCase (some with spaces) to snake_case. Update all Lua code that constructs paths from def_id to use a name-normalization function. Fix MISSSION_CONTROL.md filename typo.

## Purpose
Eliminate spaces in directory names and establish consistent snake_case naming for asset directories, matching the existing convention in data/units/ (e.g., elvish_archer.toml).

## Output
- 16 renamed directories under norrust_love/assets/units/
- Updated sprite.toml id fields
- assets.lua with def_id → directory name normalization
- generate_sprites.lua with snake_case output paths
- MISSION_CONTROL.md (renamed from MISSSION_CONTROL.md)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@norrust_love/assets.lua (path construction: line 47 `local unit_dir = dir .. "/" .. def_id`)
@norrust_love/generate_sprites.lua (def_id string literals for all 16 units)
@norrust_love/animation.lua (line 7 comment referencing "assets/units/Spearman")
</context>

<acceptance_criteria>

## AC-1: No spaces in any directory or file names
```gherkin
Given the norrust repository
When searching for directories or files with spaces in names
Then zero results are found (find . -name "* *" returns empty)
```

## AC-2: Asset directories use snake_case
```gherkin
Given the norrust_love/assets/units/ directory
When listing all subdirectories
Then all names are snake_case (e.g., elvish_archer, heavy_infantryman, spearman)
```

## AC-3: Love2D game loads all unit sprites correctly
```gherkin
Given the renamed directories and updated Lua code
When the game renders units with sprites (Spearman, Elvish Archer, etc.)
Then all 16 unit sprites load from the new snake_case paths (no fallback to colored circles)
```

## AC-4: Rust tests still pass
```gherkin
Given no Rust source changes
When running cargo test
Then all 97 tests pass (no regressions)
```

## AC-5: MISSSION_CONTROL.md typo fixed
```gherkin
Given the root directory
When looking for MISSSION_CONTROL.md (triple 's')
Then it no longer exists, replaced by MISSION_CONTROL.md
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Rename directories and update sprite.toml files</name>
  <files>
    norrust_love/assets/units/* (16 directories)
    norrust_love/assets/units/*/sprite.toml (16 files)
    MISSSION_CONTROL.md → MISSION_CONTROL.md
  </files>
  <action>
    Rename all 16 unit asset directories to snake_case:
    - "Elvish Archer" → elvish_archer
    - "Elvish Captain" → elvish_captain
    - "Elvish Fighter" → elvish_fighter
    - "Elvish Scout" → elvish_scout
    - "Elvish Shaman" → elvish_shaman
    - "Heavy Infantryman" → heavy_infantryman
    - "Orcish Archer" → orcish_archer
    - "Orcish Assassin" → orcish_assassin
    - "Orcish Grunt" → orcish_grunt
    - "Orcish Warrior" → orcish_warrior
    - Bowman → bowman
    - Cavalryman → cavalryman
    - Lieutenant → lieutenant
    - Mage → mage
    - Sergeant → sergeant
    - Spearman → spearman

    In each sprite.toml, update the `id` field to match the new snake_case directory name.

    Rename MISSSION_CONTROL.md to MISSION_CONTROL.md (fix triple 's' typo).

    Use `git mv` for all renames so git tracks the history.
  </action>
  <verify>
    find norrust_love/assets/units -maxdepth 1 -type d | sort
    find . -name "* *" | head -5  (should return empty)
    ls MISSION_CONTROL.md (exists)
    ls MISSSION_CONTROL.md (does not exist)
  </verify>
  <done>AC-1, AC-2, AC-5 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Add name normalization to assets.lua and update generate_sprites.lua</name>
  <files>
    norrust_love/assets.lua
    norrust_love/generate_sprites.lua
    norrust_love/animation.lua (comment only)
  </files>
  <action>
    In assets.lua:
    - Add a local function `normalize_unit_dir(def_id)` that converts a def_id like "Elvish Archer" or "Spearman" to snake_case directory name "elvish_archer" or "spearman".
      Implementation: lowercase the string, replace spaces with underscores.
      `return def_id:lower():gsub(" ", "_")`
    - Update line 47 (and any other path construction using def_id for directory lookup) to use `normalize_unit_dir(def_id)` instead of raw def_id.

    In generate_sprites.lua:
    - Update all `def_id` string literals for output directory names to use snake_case:
      "Elvish Archer" → "elvish_archer", "Spearman" → "spearman", etc.
    - OR add the same normalize function and apply it when constructing output paths.
    - The def_id used for display/metadata can stay human-readable if desired, but the output directory path must be snake_case.

    In animation.lua:
    - Update comment on line 7 from "assets/units/Spearman" to "assets/units/spearman".

    IMPORTANT: Do NOT change unit def_ids in TOML data files, Rust code, faction files, scenario files, or recruit group files. The game data names ("Elvish Archer") remain unchanged. Only the file system paths and the Lua code that constructs those paths change.
  </action>
  <verify>
    grep -n "normalize_unit_dir" norrust_love/assets.lua  (function exists)
    grep -rn "Elvish Archer\|Orcish Grunt\|Heavy Infantryman" norrust_love/assets.lua  (no raw spaced names in path construction)
    cargo test  (97 tests pass — no Rust changes)
  </verify>
  <done>AC-3, AC-4 satisfied (sprites load from new paths, Rust unchanged)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Renamed 16 asset directories to snake_case, updated Lua path construction with normalize function</what-built>
  <how-to-verify>
    1. Run: cd norrust_love && love .
    2. Start a game (any scenario)
    3. Verify: All units with sprites (Spearman, Elvish Archer, Orcish Grunt, etc.) render with their spritesheet graphics, NOT fallback colored circles
    4. Check: Unit panel sidebar shows portrait for inspected units
    5. Confirm: No errors in terminal output related to missing files
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_core/src/* (no Rust changes)
- data/units/*.toml (unit IDs stay as-is: "Elvish Archer", "Spearman", etc.)
- data/factions/*.toml (leader_def values unchanged)
- data/recruit_groups/*.toml (member names unchanged)
- scenarios/*.toml (unit_type values unchanged)
- norrust_love/main.lua (no changes)
- norrust_love/norrust.lua (no changes)
- norrust_love/viewer.lua (if it references asset dirs, update only path construction)

## SCOPE LIMITS
- Only rename asset directories and fix path construction in Lua
- Do not rename TOML data files (they're already snake_case)
- Do not change game data IDs — only file system layout
- Do not refactor main.lua (that's Phase 41)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `find . -name "* *"` returns no results
- [ ] All 16 directories under norrust_love/assets/units/ are snake_case
- [ ] `cargo test` passes all 97 tests
- [ ] Love2D game renders all 16 unit sprites correctly (human verified)
- [ ] MISSION_CONTROL.md exists, MISSSION_CONTROL.md does not
- [ ] No Rust source files modified
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No spaces in any directory or file names
- Game renders identically to before (sprites load from new paths)
</success_criteria>

<output>
After completion, create `.paul/phases/40-asset-directory-naming/40-01-SUMMARY.md`
</output>
