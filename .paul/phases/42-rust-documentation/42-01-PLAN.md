---
phase: 42-rust-documentation
plan: 01
type: execute
wave: 1
depends_on: ["41-01"]
files_modified:
  - norrust_core/src/lib.rs (module doc)
  - norrust_core/src/schema.rs (6 struct docs + module doc)
  - norrust_core/src/loader.rs (trait + struct + 3 method docs + module doc)
  - norrust_core/src/combat.rs (CombatPreview + next_u64 + module doc)
  - norrust_core/src/ffi.rs (NorRustEngine doc)
  - norrust_core/src/snapshot.rs (from_game_state + module doc)
  - norrust_core/src/unit.rs (Unit::new + module doc)
  - norrust_core/src/game_state.rs (GameState::new + module doc)
  - norrust_core/src/board.rs (Board::new + module doc)
  - norrust_core/src/hex.rs (module doc)
  - norrust_core/src/pathfinding.rs (module doc)
  - norrust_core/src/ai.rs (module doc)
  - norrust_core/src/scenario.rs (module doc)
  - norrust_core/src/mapgen.rs (module doc)
autonomous: true
---

<objective>
## Goal
Add doc comments (///) to all ~27 undocumented public items and module-level docs (//!) to all 12 files missing them. Follow existing documentation style.

## Purpose
Complete Rust documentation enables `cargo doc` to generate useful API reference. Future contributors can understand the codebase without reading implementation details.

## Output
- All public items in norrust_core/src/ have /// doc comments
- All modules have //! module-level doc comments
- `cargo doc` generates clean output with no warnings about missing docs
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@norrust_core/src/ (all .rs files — audit identified 27 undocumented public items across 10 files)
</context>

<acceptance_criteria>

## AC-1: All public items documented
```gherkin
Given all Rust source files in norrust_core/src/
When searching for pub items without /// comments
Then zero undocumented public items remain
```

## AC-2: All modules have module-level docs
```gherkin
Given all Rust source files in norrust_core/src/
When checking for //! module doc comments
Then every .rs file starts with a //! module doc comment
```

## AC-3: Existing documentation unchanged
```gherkin
Given the existing doc comments in the codebase
When adding new documentation
Then no existing doc comments are modified or removed
```

## AC-4: All tests still pass
```gherkin
Given no logic changes (documentation only)
When running cargo test
Then all 97 tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add module-level docs and document schema.rs + loader.rs</name>
  <files>norrust_core/src/lib.rs, norrust_core/src/schema.rs, norrust_core/src/loader.rs, norrust_core/src/hex.rs, norrust_core/src/pathfinding.rs, norrust_core/src/ai.rs, norrust_core/src/scenario.rs, norrust_core/src/mapgen.rs</files>
  <action>
    Add //! module-level doc comments to these 8 files (ffi.rs and campaign.rs already have them).

    Module doc style (1-2 lines, matches existing ffi.rs/campaign.rs style):
    ```rust
    //! Brief description of what this module provides.
    //! Additional context if needed.
    ```

    Document undocumented items in schema.rs (6 structs):
    - `AttackDef` — "Attack definition loaded from TOML unit files."
    - `UnitPlacement` — "Unit placement entry for scenario unit files."
    - `TriggerSpawnDef` — "Spawn definition within a trigger zone."
    - `UnitsDef` — "Container for unit placements loaded from TOML."
    - `RecruitGroup` — "Named group of recruitable unit types for faction definitions."
    - `FactionDef` — "Faction definition loaded from TOML."

    Document undocumented items in loader.rs:
    - `IdField` trait — "Trait for types that have a string ID field, enabling generic registry loading."
    - `Registry<T>` struct — "Generic registry that loads and stores TOML-defined game data by ID."
    - `Registry::get()` — "Look up an entry by its ID. Returns None if not found."
    - `Registry::all()` — "Return a slice of all loaded entries."
    - `Registry::len()` — "Return the number of loaded entries."

    DO NOT modify any existing doc comments or code logic.
  </action>
  <verify>
    grep -c "//!" on all modified files (each should have at least 1)
    grep "pub struct\|pub trait\|pub enum\|pub fn" schema.rs loader.rs — verify all have /// above them
  </verify>
  <done>AC-1 partially, AC-2 partially satisfied (schema + loader + 8 module docs)</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining undocumented items across all other files</name>
  <files>norrust_core/src/ffi.rs, norrust_core/src/snapshot.rs, norrust_core/src/unit.rs, norrust_core/src/combat.rs, norrust_core/src/game_state.rs, norrust_core/src/board.rs</files>
  <action>
    Add //! module docs to files that need them (snapshot.rs, unit.rs, combat.rs, game_state.rs, board.rs).
    ffi.rs already has //! — skip module doc.

    Document these specific items:

    ffi.rs:
    - `NorRustEngine` struct — "Opaque engine handle exposed through the C ABI."

    snapshot.rs:
    - `StateSnapshot::from_game_state()` — "Build a snapshot from the current game state, capturing all units, terrain, gold, and win condition data."

    unit.rs:
    - `Unit::new()` — "Create a new unit with the given ID, definition ID, faction, and position."

    combat.rs:
    - `Rng::next_u64()` — "Generate the next pseudorandom u64 using xorshift64."
    - `CombatPreview` struct — "Result of a Monte Carlo combat simulation, containing damage distributions and kill probabilities for both sides."

    game_state.rs:
    - `GameState::new()` — "Create a new empty game state with a random seed."

    board.rs:
    - `Board::new()` — "Create a new empty board with the given dimensions."

    DO NOT modify any existing doc comments or code logic.
    After both tasks, run cargo test to verify no breakage.
  </action>
  <verify>
    cargo test (97 tests pass)
    grep for any remaining "pub fn\|pub struct\|pub enum\|pub trait" without preceding /// — should be 0
  </verify>
  <done>AC-1, AC-2, AC-3, AC-4 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Any existing doc comments (only ADD, never modify)
- Any code logic, function signatures, or types
- norrust_love/* (Lua documentation is Phase 43)
- Test code documentation (test modules are internal)

## SCOPE LIMITS
- Documentation only — no refactoring, no new features
- Follow existing style (see hex.rs, pathfinding.rs, campaign.rs for reference)
- Keep doc comments concise (1-3 lines per item, matching existing style)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All .rs files have //! module doc comments
- [ ] All pub items have /// doc comments
- [ ] `cargo test` passes all 97 tests
- [ ] No existing documentation was modified
- [ ] Doc style matches existing patterns (hex.rs, campaign.rs)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Zero undocumented public items remain
- 97 tests still passing
</success_criteria>

<output>
After completion, create `.paul/phases/42-rust-documentation/42-01-SUMMARY.md`
</output>
