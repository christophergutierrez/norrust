---
phase: 111-faction-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["campaigns/tutorial.toml"]
autonomous: true
---

<objective>
## Goal
Fix the tutorial campaign's faction reference from "orcs" to "northerners" and verify all 4 factions load, recruit, and advance correctly end-to-end.

## Purpose
Completes v3.8 Unit Expansion — all 4 factions (loyalists, rebels, northerners, undead) are fully playable with correct advancement trees, recruit groups, and campaign references.

## Output
- Fixed `campaigns/tutorial.toml` with correct faction ID
- All tests passing (unit + integration)
- End-to-end verification of faction loading and advancement
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/109-toml-completion/109-01-SUMMARY.md — All 95 TOMLs complete, 4 factions defined, recruit groups wired

## Source Files
@campaigns/tutorial.toml
@data/factions/loyalists.toml
@data/factions/rebels.toml
@data/factions/northerners.toml
@data/factions/undead.toml
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: Campaign References Correct
```gherkin
Given the tutorial campaign definition
When loaded by the engine
Then faction_1 resolves to "northerners" (not "orcs")
```

## AC-2: All 4 Factions Load Successfully
```gherkin
Given the faction registry with loyalists, rebels, northerners, undead
When the engine loads all factions
Then each faction has a valid leader_def and non-empty recruit group
```

## AC-3: All Unit Tests Pass
```gherkin
Given the full test suite
When cargo test runs
Then all unit and integration tests pass with no regressions
```

## AC-4: Advancement Paths Verified
```gherkin
Given all 95 unit TOMLs with advances_to fields
When the engine validates advancement chains
Then every advances_to target resolves to a valid unit definition
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix campaign tutorial.toml faction reference</name>
  <files>campaigns/tutorial.toml</files>
  <action>
    Change line 5 from `faction_1 = "orcs"` to `faction_1 = "northerners"`.
    This is the last remaining "orcs" reference in the codebase — all Rust tests
    were already updated in Phase 109.
  </action>
  <verify>grep -r '"orcs"' campaigns/ data/factions/ norrust_core/src/ norrust_core/tests/ — should return zero matches</verify>
  <done>AC-1 satisfied: Campaign references "northerners" correctly</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify all factions and advancement</name>
  <files>(no modifications — verification only)</files>
  <action>
    Run the full test suite:
    1. `cargo test --lib` — all unit tests including faction/recruit-group registry loading
    2. `cargo test --test simulation --test test_ffi --test scenario_validation` — integration tests

    The existing tests already cover:
    - test_faction_registry_loads: verifies 4 factions load with correct IDs
    - test_recruit_group_registry_loads: verifies 4 recruit groups
    - balance tests: run full games with loyalists vs northerners
    - scenario_validation: validates all scenarios load correctly
    - test_ffi: exercises FFI with northerners faction

    If any test fails, diagnose and fix.
  </action>
  <verify>All tests pass with exit code 0</verify>
  <done>AC-2, AC-3, AC-4 satisfied: All factions load, tests pass, advancement validated</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- data/units/**/*.toml (unit definitions locked from Phase 109)
- data/factions/*.toml (faction definitions locked from Phase 109)
- data/recruit_groups/*.toml (recruit groups locked from Phase 109)
- norrust_core/src/loader.rs (loader logic stable)
- tools/generate_sprites.py (sprite tool locked from Phase 110)

## SCOPE LIMITS
- No sprite generation (deferred — circles serve as placeholders)
- No new scenarios or campaigns
- No Lua/Love2D changes (faction UI already data-driven)
- No new unit definitions

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `grep -r '"orcs"' campaigns/ data/factions/ norrust_core/src/ norrust_core/tests/` returns no matches
- [ ] `cargo test --lib` passes
- [ ] `cargo test --test simulation --test test_ffi --test scenario_validation` passes
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No "orcs" references remain in code/data
- All 4 factions fully operational
</success_criteria>

<output>
After completion, create `.paul/phases/111-faction-integration/111-01-SUMMARY.md`
</output>
