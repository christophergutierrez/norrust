# Phase 136 — Compact observation summary

The default client projection is `compact_v1`; `--diagnostic` restores the
legacy full snapshot. The compact briefing carries authoritative instance
facts, occupied terrain labels, and a reduced turn-options projection while
preserving untrusted-data delimiters. No legality is reconstructed in Python.

Historical corpus byte measurements remain unavailable because the source
eight-call log is not present in this checkout.
