---
phase: 117-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["debug/data/**"]
autonomous: false
---

<objective>
## Goal
Run all non-balance tests, regenerate debug data, and verify all 4 factions recruit and advance correctly in-game.

## Purpose
Final validation gate for v4.0 Unit Content Completeness. Confirms the General unit addition (Phase 115) and stat verification (Phase 116) result in a fully functional game with all advancement chains working end-to-end.

## Output
- All non-balance tests passing
- Regenerated debug/data/ reflecting current unit tree
- In-game verification of all 4 factions
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/115-missing-units-cleanup/115-01-SUMMARY.md (General created)
@.paul/phases/116-stat-verification/116-01-SUMMARY.md (stats verified)

## Source Files
@tools/generate_debug.py (debug data generator)
@debug/debug_config.toml (debug overrides)
</context>

<skills>
No specialized flows configured.
</skills>

<acceptance_criteria>

## AC-1: All Non-Balance Tests Pass
```gherkin
Given the full test suite excluding balance tests
When cargo test --lib and cargo test --test test_ffi --test scenario_validation --test simulation -- --skip balance are run
Then all tests pass with zero failures
```

## AC-2: Debug Data Regenerated
```gherkin
Given the current unit tree with 132 units including General
When python3 tools/generate_debug.py --clean is run
Then debug/data/ is regenerated with patched unit TOMLs matching debug_config.toml
```

## AC-3: All 4 Factions Work In-Game
```gherkin
Given the debug sandbox with --debug flag
When each faction's leader is placed on a keep with gold
Then all 4 factions can recruit their faction units and advance them
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Run all non-balance tests</name>
  <files>None (verification only)</files>
  <action>
    Run the full test suite excluding balance tests:

    1. cargo test --lib (all unit tests)
    2. cargo test --test test_ffi --test scenario_validation --test simulation --test campaign --test dialogue -- --skip balance

    CRITICAL: Do NOT run cargo test without filters. Do NOT run balance tests. Ever.

    Report: total tests run, pass/fail count.
  </action>
  <verify>
    All tests pass with zero failures.
  </verify>
  <done>AC-1 satisfied: all non-balance tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate debug data</name>
  <files>debug/data/**</files>
  <action>
    Run: python3 tools/generate_debug.py --clean

    This regenerates debug/data/ from the current data/ tree with debug_config.toml patches applied.
    The General unit and any other changes from Phase 115 will be included.
  </action>
  <verify>
    1. debug/data/units/ contains General TOML (ls debug/data/units/lieutenant/general/)
    2. python3 tools/generate_debug.py runs without errors
  </verify>
  <done>AC-2 satisfied: debug data regenerated with current unit tree</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete unit tree with all 4 factions validated by tests + debug data regenerated</what-built>
  <how-to-verify>
    1. Run: love norrust_love --debug
    2. Start a debug scenario (any)
    3. Press G for gold, then R to recruit — verify Loyalist units appear
    4. End turns until red faction recruits — verify faction 1 (Undead) recruits successfully
    5. Optionally: press X on a unit to max XP, then attack to trigger advancement
    6. Confirm: no crashes, recruitment works for all factions shown
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_core/ Rust source code
- Existing unit TOMLs in data/units/
- Test files
- Scenario files
- tools/generate_debug.py (use as-is)

## SCOPE LIMITS
- Verification and regeneration only — no new code
- Do not fix test failures by modifying tests (fix underlying issues if any)
- No balance test runs under any circumstances
- No sprite work

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All non-balance tests pass
- [ ] debug/data/ regenerated
- [ ] User verified in-game faction recruitment
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All non-balance tests pass
- Debug data regenerated with complete unit tree
- User confirms all factions work in-game
</success_criteria>

<output>
After completion, create `.paul/phases/117-integration-validation/117-01-SUMMARY.md`
</output>
