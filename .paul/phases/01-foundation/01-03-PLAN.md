---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - norrust_core/src/gdext_node.rs
  - norrust_client/scripts/test_bridge.gd
autonomous: false
---

<objective>
## Goal

Connect the `Registry<T>` data loader (Plan 01-01) to the `NorRustCore` GodotClass
(Plan 01-02). Expose `load_data(path)` and `get_unit_max_hp(id)` functions to GDScript,
then verify the full round-trip: Disk → TOML → Rust Registry → GDExtension → Redot console.

## Purpose

This is the capstone of Phase 1. It proves the complete data pipeline: TOML files on disk
are parsed by Rust and the values are queryable from GDScript. Every future game mechanic
will use this same path.

## Output

- `NorRustCore` holds `Option<Registry<UnitDef>>` and `Option<Registry<TerrainDef>>`
- `load_data(path: GString) -> bool` populates the registries from disk
- `get_unit_max_hp(unit_id: GString) -> i32` queries the loaded unit data
- Redot output prints: `Fighter HP: 30` and `Archer HP: 25` — values from TOML files
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-foundation/01-01-SUMMARY.md
@.paul/phases/01-foundation/01-02-SUMMARY.md

## Source Files
@norrust_core/src/gdext_node.rs
@norrust_core/src/loader.rs
@norrust_core/src/schema.rs
@norrust_client/scripts/test_bridge.gd
</context>

<acceptance_criteria>

## AC-1: Registry Loads via GDExtension

```gherkin
Given NorRustCore is instantiated in GDScript
When load_data(path_to_data_dir) is called with the correct path
Then it returns true
And the internal unit registry contains "fighter" and "archer"
```

## AC-2: Unit Data Queryable from GDScript

```gherkin
Given load_data() returned true
When get_unit_max_hp("fighter") is called from GDScript
Then it returns 30 (the value from data/units/fighter.toml)
When get_unit_max_hp("archer") is called
Then it returns 25 (the value from data/units/archer.toml)
When get_unit_max_hp("dragon") is called (nonexistent unit)
Then it returns -1
```

## AC-3: Full Round-Trip Verified in Redot Console

```gherkin
Given the Redot project runs with the updated test_bridge.gd
When the Output panel is observed
Then it shows:
  Core version: 0.1.0
  Fighter HP: 30
  Archer HP: 25
And these values originate from TOML files on disk, not hardcoded in GDScript
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend NorRustCore with Registry fields and data-loading API</name>
  <files>norrust_core/src/gdext_node.rs</files>
  <action>
    Replace the contents of norrust_core/src/gdext_node.rs with the following
    (preserve the existing get_core_version, add the new fields and methods):

    use crate::loader::Registry;
    use crate::schema::{TerrainDef, UnitDef};
    use godot::prelude::*;

    struct NorRustExtension;

    #[gdextension]
    unsafe impl ExtensionLibrary for NorRustExtension {}

    #[derive(GodotClass)]
    #[class(base=Node)]
    pub struct NorRustCore {
        base: Base<Node>,
        units: Option<Registry<UnitDef>>,
        terrain: Option<Registry<TerrainDef>>,
    }

    #[godot_api]
    impl INode for NorRustCore {
        fn init(base: Base<Node>) -> Self {
            Self { base, units: None, terrain: None }
        }
    }

    #[godot_api]
    impl NorRustCore {
        #[func]
        fn get_core_version(&self) -> GString {
            "0.1.0".into()
        }

        /// Load all game data from `data_path/units/` and `data_path/terrain/`.
        /// Returns true on success, false on any IO or parse error.
        /// `data_path` should be an absolute or resolvable path (use
        /// ProjectSettings.globalize_path("res://") + "/../data" from GDScript).
        #[func]
        fn load_data(&mut self, data_path: GString) -> bool {
            use std::path::PathBuf;
            let base = PathBuf::from(data_path.to_string());

            match Registry::<UnitDef>::load_from_dir(&base.join("units")) {
                Ok(registry) => self.units = Some(registry),
                Err(e) => {
                    godot_error!("load_data: failed to load units: {}", e);
                    return false;
                }
            }

            match Registry::<TerrainDef>::load_from_dir(&base.join("terrain")) {
                Ok(registry) => self.terrain = Some(registry),
                Err(e) => {
                    godot_error!("load_data: failed to load terrain: {}", e);
                    return false;
                }
            }

            godot_print!(
                "load_data: loaded {} units, {} terrain types",
                self.units.as_ref().map(|r| r.len()).unwrap_or(0),
                self.terrain.as_ref().map(|r| r.len()).unwrap_or(0),
            );
            true
        }

        /// Returns max_hp for the unit, or -1 if not found or data not loaded.
        #[func]
        fn get_unit_max_hp(&self, unit_id: GString) -> i32 {
            self.units
                .as_ref()
                .and_then(|r| r.get(&unit_id.to_string()))
                .map(|u| u.max_hp as i32)
                .unwrap_or(-1)
        }
    }

    Avoid:
    - Panicking on missing data — return -1 or false gracefully
    - Using unwrap() outside of tests
    - Adding any game logic — this is a data access bridge only
  </action>
  <verify>
    cd norrust_core && cargo build 2>&1 | tail -5
    # Expect: Finished with no errors
    cd norrust_core && cargo test 2>&1 | tail -5
    # Expect: 3 passed; 0 failed (existing tests unaffected)
    cd norrust_core && cargo clippy -- -D warnings 2>&1 | tail -5
    # Expect: Finished with no warnings
  </verify>
  <done>AC-1 and AC-2 preconditions met: load_data and get_unit_max_hp compiled; existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update test_bridge.gd to exercise the data pipeline</name>
  <files>norrust_client/scripts/test_bridge.gd</files>
  <action>
    Replace norrust_client/scripts/test_bridge.gd with:

      extends Node

      func _ready():
          var core = NorRustCore.new()
          add_child(core)

          print("Core version: ", core.get_core_version())

          # Build absolute path to the data directory.
          # ProjectSettings.globalize_path("res://") returns the absolute path to
          # the norrust_client/ project root. The data directory is one level up.
          var project_dir = ProjectSettings.globalize_path("res://")
          var data_path = project_dir + "/../data"

          if core.load_data(data_path):
              print("Fighter HP: ", core.get_unit_max_hp("fighter"))
              print("Archer HP: ", core.get_unit_max_hp("archer"))
              print("Dragon HP: ", core.get_unit_max_hp("dragon"))
          else:
              push_error("Failed to load data from: " + data_path)

          core.queue_free()

    Avoid:
    - Hardcoding HP values in GDScript (values must come from Rust/TOML)
    - Modifying test_scene.tscn — the scene reference to this script stays the same
  </action>
  <verify>
    cat norrust_client/scripts/test_bridge.gd
    # Confirm load_data and get_unit_max_hp calls are present
  </verify>
  <done>AC-3 precondition met: GDScript calls load_data() and queries unit HP</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    NorRustCore now loads TOML registries from disk and exposes unit data to GDScript.
    The full pipeline: disk → TOML → Rust Registry → GDExtension → Redot console.
  </what-built>
  <how-to-verify>
    1. Rebuild the Rust library:
         cd norrust_core && cargo build

    2. Copy the updated .so to the Redot project:
         cp target/debug/libnorrust_core.so ../norrust_client/bin/
         (or the symlink from Plan 01-02 already points to the latest build)

    3. Run the Redot project (F5 in editor, or re-run if already open):
         Expected Output panel:
           Core version: 0.1.0
           load_data: loaded 2 units, 2 terrain types
           Fighter HP: 30
           Archer HP: 25
           Dragon HP: -1

    4. Confirm:
         - "Fighter HP: 30" matches data/units/fighter.toml (max_hp = 30) ✓
         - "Archer HP: 25" matches data/units/archer.toml (max_hp = 25) ✓
         - "Dragon HP: -1" confirms graceful not-found handling ✓
         - "load_data: loaded 2 units, 2 terrain types" confirms both registries loaded ✓
  </how-to-verify>
  <resume-signal>Type "approved" if all four lines match, or describe any errors to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_core/src/schema.rs — structs locked from 01-01
- norrust_core/src/loader.rs — Registry<T> locked from 01-01
- data/units/ and data/terrain/ — TOML data files locked from 01-01
- norrust_client/scenes/test_scene.tscn — scene reference to script stays the same
- norrust_client/norrust_core.gdextension — extension config unchanged

## SCOPE LIMITS
- No terrain query functions yet (get_terrain_defense etc.) — Phase 2
- No new GDExtension classes — NorRustCore is the only bridge class this phase
- No faction data loading — deferred to Phase 2

</boundaries>

<verification>
Before checkpoint:
- [ ] `cd norrust_core && cargo build` exits 0
- [ ] `cd norrust_core && cargo test` — all 3 tests pass
- [ ] `cd norrust_core && cargo clippy -- -D warnings` exits 0
- [ ] `cat norrust_client/scripts/test_bridge.gd` shows load_data() and get_unit_max_hp() calls

At checkpoint:
- [ ] Output shows "load_data: loaded 2 units, 2 terrain types"
- [ ] Output shows "Fighter HP: 30"
- [ ] Output shows "Archer HP: 25"
- [ ] Output shows "Dragon HP: -1"
</verification>

<success_criteria>
- cargo test 3/3 pass, clippy clean
- Redot output confirms all 4 lines including -1 for nonexistent unit
- Values (30, 25) come from TOML files, not GDScript
- Phase 1 sub-phase 1.5 complete: full Disk → TOML → Rust → GDScript round-trip proven
</success_criteria>

<output>
After completion, create `.paul/phases/01-foundation/01-03-SUMMARY.md` documenting:
- Round-trip validation results
- Any path resolution surprises (project_dir format, trailing slashes, etc.)
- Phase 1 completion status
</output>
