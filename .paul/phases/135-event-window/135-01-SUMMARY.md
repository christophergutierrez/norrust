# Phase 135 — Event window summary

The client keeps a full event log for diagnostics and a bounded observation
window for prompts. `--event-window-observations` selects how many observation
intervals are included; the default is one. The real driver smoke run remains
green. A historical eight-call byte corpus is unavailable in this checkout.
