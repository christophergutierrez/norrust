---
phase: 75-background-music
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/sounds/menu_music.ogg (moved from battle_background.ogg)
  - norrust_love/main.lua
autonomous: false
---

<objective>
## Goal
Add menu music that loops on the scenario select screen, with proper transitions to per-scenario music during gameplay and back to menu music when returning.

## Purpose
Music creates atmosphere. Menu music welcomes players; per-scenario music sets the tone for each battle.

## Output
- Menu music plays on scenario select screen (looping)
- Music stops/transitions when scenario loads (per-scenario music if available)
- Menu music resumes when returning to scenario select
</objective>

<context>
## Source Files
@norrust_love/main.lua — game_mode states, love.load, scenario_loaded event
@norrust_love/sound.lua — play_music(path), stop_music() already exist
</context>

<acceptance_criteria>

## AC-1: Menu music plays on scenario select
```gherkin
Given the game launches
When the scenario select screen is displayed
Then menu music loops continuously
```

## AC-2: Menu music stops when scenario starts
```gherkin
Given menu music is playing
When a scenario loads
Then menu music stops (per-scenario music plays if music.ogg exists)
```

## AC-3: Menu music resumes on return
```gherkin
Given a scenario ends (win/loss) or player returns to menu
When the scenario select screen is displayed again
Then menu music resumes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Move music file and wire menu music</name>
  <files>data/sounds/menu_music.ogg, norrust_love/main.lua</files>
  <action>
    1. Move battle_background.ogg to data/sounds/menu_music.ogg
    2. In love.load(), after sound.load(), start menu music:
       `shared.sound.play_music("data/sounds/menu_music.ogg")`
    3. In scenario_loaded event handler, the existing play_music call handles
       per-scenario music (stops previous music automatically via stop_music inside play_music).
       If no scenario music.ogg exists, play_music returns without playing — but menu music
       was already stopped. This is correct: silence during gameplay with no music.ogg.
    4. When returning to PICK_SCENARIO (game over → menu transitions), restart menu music.
       Find all places where `game_mode = PICK_SCENARIO` is set after gameplay:
       - Line ~1160: campaign complete
       - Line ~1168: individual scenario end
       Add `shared.sound.play_music("data/sounds/menu_music.ogg")` at each.
  </action>
  <verify>luajit -bl norrust_love/main.lua /dev/null</verify>
  <done>AC-1, AC-2, AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Menu music on scenario select, transitions to/from gameplay</what-built>
  <how-to-verify>
    1. Run: cd norrust_love && love .
    2. Hear music on scenario select screen (looping)
    3. Start a scenario — menu music should stop
    4. If scenario has music.ogg, scenario music plays; otherwise silence
    5. End game (win or lose) — menu music should resume on return to select screen
    6. Press M to mute — music should mute; unmute should resume
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- norrust_love/sound.lua — infrastructure already correct
- norrust_core/ — no Rust changes
- Scenario music loading (Phase 72) — already wired

## SCOPE LIMITS
- No fade transitions (simple stop/play)
- No music volume separate from master volume

</boundaries>

<verification>
- [ ] luajit -bl norrust_love/main.lua /dev/null — syntax clean
- [ ] Menu music plays on launch
- [ ] Music transitions work
</verification>

<success_criteria>
- Menu music loops on scenario select
- Proper transitions during gameplay
- User approves
</success_criteria>

<output>
After completion, create `.paul/phases/75-background-music/75-01-SUMMARY.md`
</output>
